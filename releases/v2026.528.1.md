# v2026.528.1

> Released: 2026-05-28

## UI

- **Favicon: dark-square briefcase across all formats** — `favicon.svg`, `favicon.ico`, `favicon-16x16.png`, and `favicon-32x32.png` are all regenerated from `apple-touch-icon.png` so every browser format renders the same dark-rounded-square + filled-briefcase design. Re-runnable any time via `node scripts/regen-favicons.mjs`.

## Install

```bash
npx dealdesk@latest onboard
```
